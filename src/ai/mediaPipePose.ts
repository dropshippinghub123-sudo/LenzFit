// MediaPipe Pose integration stub (to be replaced with actual WASM/native binding)
import { Keypoint } from './poseEngine';

export async function runMediaPipePose(imageData: ImageData | HTMLCanvasElement): Promise<Keypoint[]> {
  // TODO: Integrate MediaPipe WASM or native module for pose detection
  // Return array of keypoints with {name, x, y, confidence}
  return [];
}
